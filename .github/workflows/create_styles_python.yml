name: Run Python Script
on:
  push:
#    paths:
#    - 'example.bib'
jobs:
  replace:
    name: replace
    runs-on: ubuntu-latest
    steps:
    - name: Check-out
      uses: actions/checkout@v2
    - name: run
      run: |
        python replace.py
        for ((i=0; i < 2; i++)) do printf test$i.tex; done
      #  ls *.tex
      #  cat test*.tex
        
